//1234567890	=>	1,234,567,890

function format(v) {

  //匹配规则 （？=）表示正向肯定预查,先假定后面的成立

  const reg = /\d{1,3}(?=(\d{3})+$)/g

  //反单号转化为字符串，'$&'表示当前正则表达式匹配到的数字

  return `${v}`.replace(reg, '$&,')

}
console.log(format(1234567890));